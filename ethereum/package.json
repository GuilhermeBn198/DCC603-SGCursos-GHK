{
  "name": "contracts",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "compile": "truffle compile --host=ganache",
    "migrate": "truffle migrate --host=ganache",
    "start": "truffle compile --host=ganache && truffle migrate --host=ganache && node app.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "web3": "^1.10.0"
  }
}
