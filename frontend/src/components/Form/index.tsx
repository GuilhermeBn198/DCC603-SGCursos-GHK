'use client'
import { styled } from 'styled-components'

export const Form = styled.form`
  display: flex;
  flex-direction: column;
  gap: 16px;

  h1 {
    font-size: 24px;
    font-weight: 700;
  }
`
